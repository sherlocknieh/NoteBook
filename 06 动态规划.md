---
要求题目:2,3,7,9;
---

# 第6章 动态规划

**6.1**

分析求 Fibonacci 序列递归算法 F(n) 的时间复杂度。

**答:**

> 根据递归树高度为 n，每层的节点数至多为 2^n，估计时间复杂度为 $O(2^n)$。  
>
> 代入递归方程 $F(n) = F(n-1) + F(n-2)$。
>
> 得 $F(n) = O(2^{n-1}) + O(2^{n-2}) = O(2^n)$。

### 装配线调度问题

- [x] **6.2**

说明应如何修改程序 `PrintStations`，使它能以装配点的递增顺序输出各装配点。提示：使用递归。

```python
# 书上写的 l* 不合实际语法, 这里改用 L 代替
def PrintStations(l, n):
    i = L
    print('line', i, 'station', n)
    for j in range(n, 2, -1):
        i = l[i][j]
        print('line', i, 'station', j-1)
```

**答:**

```python
def PrintStations(l, n, L):
    if n == 2:
        print('line', L, 'station', n)
        return
    PrintStations(l, n-1, l[L][n])
    print('line', L, 'station', n)
```


- [x] **6.3**

保存 f 和 l 值的表，总大小为 4n-2，说明如何把表的大小缩减到 2n+2，使之仍然能够计算出 $f^*$，并且仍然能够构造出最快装配路线。

**答:**

> 数组 a1, a2 中每个装配点的值只用一次就不再使用了，因此用过后可以覆盖。直接用 a1 储存 f1, a2 储存 f2, 节省 $2n$ 个空间。
>
> 编程实测 $2n - 2$ 的空间足以完成算法, 不需要 $2n+2$;

### 矩阵链乘法问题

**6.4**

对维数序列为 5,10,3,12,5,50,6 的矩阵链，找出其矩阵链乘积的一个最优加括号的方式。

**答:**

> ...


**6.5**

给定矩阵链 $A_1, A_2, ..., A_n$，由 `DPMatrixChain` 计算出的 s 以及下标 i 和 j，请设计一个求最优的矩阵链乘法的递归算法 `MatrixChainMultiply(A, s, i, j)`。（初始调用为 `MatrixChainMultiply(A, s, 1, n)`。）

**答:**

> ...


**6.6**

使用替换法证明递归方程（6.1）的解为 $\Omega(2^n)$。


$$
P(n)=
\begin{cases}

\Theta(1), & n=1 \\
\sum_{k=1}^{n-1} P(k)P(n-k), & n>1

\end{cases}

\tag{6.1}
$$


**答:**

> ...


- [ ] **6.7**

画出 `MergeSort` 算法排序 16 个元素的递归树。解释在提高一个好的分治算法，例如 `MergeSort` 的效率时，利用备忘录方法为什么没有效果。

**答:**

> MergeSort 没有重叠子问题;


**6.8**

说明装配线调度问题具有重叠子问题性质。

**答:**

> 用递归函数 FastestWay(i, n) 表示第 $i$ 条装配线的第 $n$ 站的最快装配完成时间。
> 
> 调用 FastestWay(1, n) 会触发 FastestWay(1, n-1) 和 FastestWay(2, n-1)。
> 
> 调用 FastestWay(2, n) 也会触发 FastestWay(1, n-1) 和 FastestWay(2, n-1)。
>
> FastestWay(1, n-1) 和 FastestWay(2, n-1) 被调用了两次，说明装配线调度问题具有重叠子问题性质;


### 最长公共子序列

- [ ] **6.9**

已知原始序列 $X_m = <x_1, x_2, ..., x_m>$ 和 $Y_n = <y_1, y_2, ..., y_n>$，假设已计算出表 c，说明不用表 b，也可以在 $O(m+n)$ 时间内构造一个 LCS。

**答:**

> ...


**6.10**

请为 `DPLCSLength` 算法设计出一个运行时间为 $O(mn)$ 的备忘录算法。

**答:**

> ...


**6.11**

设 X_m 和 Y_n 都是以 A 开始的字符序列; 证明 X_m 和 Y_n 的每一个最长公共子序列都从 A 开始。

**答:**

> ...


**6.12**

给定一个具有 n 个数的序列，请设计出一个运行时间为 $O(n^2)$ 的算法，使之能找出该序列中最长的单调递增子序列。能设计出运行时间为 $O(n\log n)$ 的算法吗？

**答:**

> ...


### 背包问题

**6.13**

如何修改 0/1 背包问题的动态规划算法，使得其运行时间复杂度为 $O(W)$，其中 W 为背包的最大载重量。何修改动态规划算法，使其空间复杂度降低。

**答:**

> ...


**6.14**

定义 0/1/2 背包问题为：

$\max \sum_{j=1}^{n} v_j x_j$

使得 $\sum_{j=1}^{n} w_j x_j \leq W$

其中 $x_j \in \{0, 1, 2\}$。

设其子问题的定义类似 0/1 背包问题中的定义，完成下列任务：

（1）为 0/1/2 背包问题推出类似于 0/1 背包问题的递归方程。

（2）假设 $w_j, v_j, W$ 为整数，设计一个类似于 0/1 背包的动态规划求解算法并构造出最优解。

（3）分析上述算法的时间复杂度。

**答:**

> ...


### 最优二叉搜索树

**6.15**

给定下表，请利用 `DPOptimalBST(p, q, n)` 和 `PrintBST(root, i, j)` 算法分别求出最优二叉树的代价以及相应的二叉树。

| i | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|---|
| $p_i$ | | 0.04 | 0.06 | 0.08 | 0.02 | 0.1 | 0.12 | 0.14 |
| $q_i$ | 0.06 | 0.06 | 0.06 | 0.06 | 0.05 | 0.05 | 0.05 | 0.05 |

**答:**

> ...


**6.16**

如果不利用递归方程（6.3）计算表 $w_[i,j]$，而是直接由公式（6.2）计算，试分析 `DPOptimalBST(p, q, n)` 算法时间复杂度的变化。

公式（6.2）：$w[i,j] = \sum_{k=i}^{j}p_k + \sum_{k=i-1}^{j} q_k$

**答:**

> ...


**6.17**

著名计算机科学家 Knuth 已经证明，对任何 $1 \leq i < j \leq n$，最优子树的根总有 $root[i, j-1] \leq root[i, j] \leq root[i+1, j]$ 成立。用这个结论修改 `DPOptimalBST(p, q, n)` 算法，使它的时间复杂度为 $O(n^2)$。

**答:**

> ...


### 硬币兑换问题

**6.18**

给定总金额为 A 的一张纸币，现要兑换成面额分别为 $a_1, a_2, ..., a_n$ 的硬币。硬币兑换问题是用最少枚数的硬币来兑换总金额为 A 的纸币。设计一个动态规划算法求解该问题，使得对任何可用的硬币种类总能求出最少枚数的硬币。

**答:**

> ...


### 字符串转换

**6.19**

给定两个字符串 A 和 B 以及下列三种字符运算：

- （1）删除一个字符；
- （2）插入一个字符；
- （3）将一个字符改写为另一个字符。

问题是需要求出将字符串 A 转换为字符串 B，所需要的最少的字符运算数。请设计一个有效算法，对任给的 2 个字符串 A 和 B，输出将字符串 A 转换为字符串 B 所需的最少字符运算数。

**答:**

> ...


### 其他动态规划问题

**6.20**

在一个足球场的四周摆放着 n 堆货物，现要将周边货物合并成一堆。规定每次只能选相邻的两堆货物进行合并，合并的代价为新的一堆的货物数。试设计一个算法，求出将 n 堆货物合并成一堆所需要的最小总代价。

**答:**

> ...


**6.21**

给定一个由 n 行数字组成的数字三角形，如图 6.21 所示。试设计一个算法，输出从三角形的顶至底的一条路径，使该路径经过的数字总和最大。

图 6.21 数字三角形示例:
```
         9
        / \
       3   8
      / \ / \
     8   1   0
    / \ / \ / \
   2   7   9   4
  / \ / \ / \ / \
 3   5   2   6   2
```

**答:**

> ...


**6.22**

将 $3 \times n$ 的布条切割成 $2 \times 1$ 的方块，有多少种切割方案？

**答:**

> ...


**6.23**

一个机器人每步可以走 1 米、2 米或 3 米。编写一个动态规划算法，计算机器人走 n 米，有多少种走法。

**答:**

> ...


**6.24**

n 个人参加拔河比赛，每个人有自己的重量，现在需要把他们分成两组进行比赛，每个人属于其中的一个组，两组的人员个数相差不能超过 1。为使比赛公平，求使得两组重量差最小的分配。

**答:**

> ...

## 实验题

**6.25**

分别实现矩阵链乘法的动态规划算法和备忘录算法，并用实验分析比较两种算法的效率。
