---
要求题目:;
---

# 第6章 动态规划

习题6. 16.26.36.46.56.66.76.86.992分析求Fibonacci序列递归算法F（n）的时间复杂度说明应如何修改程序PrintStations，使它能以装配点的递增顺序输出各装配点。（提示：利用递归。）保存f［和l［值的表，总大小为4n-2，说明如何把表的大小缩减到2n+2，使之仍然能够计算出*，并耳仍然能够构造出最快装配路线对维数序列为5，10.3，12，5，50，6的矩阵链，找出其矩阵链乘积的一个最优加括号的方式。给定矩阵链A,，A2，，A;，·，An，由 DPMatrixChain 计算出的 s 以及下标i和i,请设计-个求最优的矩阵链乘法的递归算法 MatrixChainMultiply（A，s，ij）。(初始调用为 MatrixChainMultiply（A，S，1,n）。）使用替换法证明递归方程（6.1）的解为0（2"）。画出 MergeSort 算法排序 16个元素的递归树。请解释在提高一个好的分治算法，例如 MergeSort 的效率时，利用备忘录方法为什么没有效果，说明装配线调度问题真有重叠子问题性质。已知原始序列Xm=<α1，α2··xm）和Y=<y1，2，·，n>，假设已计算出表c，
6.106. 116. 126. 136.146. 156. 166. 176.186. 19说明不用表b，也可以在O（m+n）时间内构造一个LCS。请为DPLCSLength算法设计出一个运行时间为O(mn）的备忘录算法设X和Y，都是以A开始的字符序列.则X和Y，的每-个最长公共子序列都从A开始,请予以证明，给定一个具有n个数的序列，请设计出一个运行时间为○（n）的算法，使之能找出该序列中最长的单调递增子序列。能设计出运行时间为○（nlgn）的算法吗？如何修改0/1背包问题的动态规划算法，使得其运行时间复杂度为O（W），其中为背包的最大载重量。如何修改动态规划算法，使其空间复杂度降低。nW定义0/1/2 背包问题为：max MV;,使得w;x ≤ W,其中x;E {0,1,2设其子问题的定义类似0/1背包问题中的定义，完成下列任务：（1）为0/1/2背包问题推出类似于0/1背包问题的递归方程。(2)假设 w;W为整数，设计一个类似于0/1背包的动态规划求解算法并构造出最优解。（3）分析上述算法的时间复杂度，给定下表，请利用 DPOptimalBST（p，，n）和 PrintBST（root，i，j）算法分别求出最优二叉树的代价以及相应的二支树。0pig;0. 0610.040.0620. 060.0630.080. 0640.020.0550.10.0560.120.0570.140.05如果不利用递归方程（6.3）计算表wii，而是直接由公式（6.2）计算，试分析DPOptimalBST(p，9，n)算法时间复杂度的变化。著名计算机科学家Knuth 已经证明，对任何 1≤i<i≤n，最优子树的根总有root［i,i－1］」≤root［i,i]」≤rooti→ 1，］]成立。用这个结论修改 DPOpti-malBST（p，9，n)算法，使它的时间复杂度为 O（n²）。给定总金额为 A 的一张纸币,现要兑换成面额分别为 a，α2，，α的硬币。硬币兑换问题是用最少枚数的硬市来兑换总金额为A的纸币。设计一个动态规划算法求解该问题，使得对任何可用的硬市种类总能求出最少枚数的硬币。给定两个字符串A和B以及下列三种字符运算：（1）删除一个字符：（2）插人个字符；（3）将一个字符改写为另一个字符。93
6.206. 216.226.23可题是需要求出将字符串A转换为符串B，所需要的最少的学符运算数。请设计一个有效算法，对任给的2个符串A和B，输出将符串A转换为字符串B所需的最少学符运算数，在一个足球场的四周摆放着n堆货物，现要将周边货物合并成一堆。规定每次只能选相邻的两堆货物进行合并，合并的代价为新的一堆的货物数试设计一个算法，求出将n堆货物合并成一堆所需要的最小总代价。给定一个由n行数字组成的数字三角形.如图6.21所示。试设计一个算法，输出从三角形的顶至底的一条路径，使该路径经过的数字总和最大。将3×n的布条切割成2×1的方块，问有多少种切割方案？个机器人每步可以走1米、2 米或3 米。编写个动态规划算法，计算机器人走n米，有多少种走法（考虑步骤的次序）。3S92图6.21626. 24几个人参加拨河比赛，每个人有自已的重量，现在需要把他们分成两组进行比赛，每个人属于其中的一个组，两组的人员个数相差不能超过1。为使比赛公平，求使得两组重量差最小的分配