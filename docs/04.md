# 第4章 递归复杂度分析

### 递归算法设计

**4.1** #TODO

设计一个递归算法，在有 $n$ 个元素的序列 $A[1..n]$ 中搜索元素 $x$。

**答:**

> ...

**4.2** #TODO

给定含有 $n$ 个元素的数组 $A$，请设计一个算法找出数组 $A$ 中出现次数最多的元素，并输出该元素出现的次数。

例如：$A = \{1,1,2,2,2,3,5\}$，$A$ 中出现次数最多的元素是 $2$，出现次数为 $3$。

**答:**

> ...

**4.3** #TODO

将第 1 章的插入排序算法改写为递归算法，并分析其时间复杂度。

**答:**

> ...

**4.4** #TODO

证明算法 `GeneratingPerm1` 的正确性。

**答:**

> ...

**4.5** #TODO

修改算法 `Perm1`，使其能够按照字典序输出排列。

**答:**

> ...

**4.6** #TODO

请仔细解释，为什么算法 `GeneratingPerm2` 中，当过程 `Perm2(m)`（$m > 0$）形式调用时，数组 $P$ 恰好包含 $m$ 个 $0$，并且 `Perm2(m-1)` 将恰好执行 $m$ 次。

**答:**

> ...

**4.7** #TODO

修改算法 `Perm2`，使数 $1,2,\ldots,n$ 的排列按算法 `Perm2` 的倒序生成。

**答:**

> ...

**4.8** #TODO

修改算法 `Perm2`，使其产生集合 $\{1,2,\ldots,n\}$ 的所有大小为 $k$ 的子集（$1 \le k \le n$），并分析算法的时间复杂度。

**答:**

> ...

### 递归方程与复杂度

**4.9** #TODO

证明递归方程 $T(n)=2T(\lfloor n/2 \rfloor + 17)+n$ 的解为 $T(n)=O(n\lg n)$。

**答:**

> ...

**4.10** #TODO

证明递归方程 $T(n)=2T(\lceil n/2 \rceil)+n$ 的解为 $T(n)=O(n\lg n)$，并证明这个递归方程的解也是 $\Omega(n\lg n)$。

**答:**

> ...

**4.11** #TODO

利用变量替换方法求解递归方程 $T(n)=2T(\sqrt{n})+1$，要求得到的解应当是渐近紧界，不必担心值是否为整数。

**答:**

> ...

**4.12** #TODO

画出递归方程 $T(n)=4T(\lfloor n/2 \rfloor)+cn$ 的递归树，并给出其解的渐近紧界；然后用替换方法证明给出的界，其中 $c$ 是一个常数。

**答:**

> ...

**4.13** #TODO

利用递归树方法估计递归方程 $T(n)=T(n-a)+T(a)+cn$ 的渐近紧界，其中 $a,c$ 是常数，且 $0<a<1,\ c>0$。

**答:**

> ...

**4.14** #TODO

证明递归方程 $T(n)=T(n/3)+T(2n/3)+O(n)$ 的解为 $T(n)=O(n\lg n)$。

**答:**

> ...

**4.15** #TODO

用公式法求解下列递归方程的渐近紧界：

1. $T(n)=4T(n/2)+n$
2. $T(n)=4T(n/2)+n^2$
3. $T(n)=4T(n/2)+n^3$

**答:**

> ...

**4.16** #TODO

公式法能否应用于递归方程 $T(n)=4T(n/2)+n^2\lg n$？为什么？给出此递归方程的渐近上界。

**答:**

> ...