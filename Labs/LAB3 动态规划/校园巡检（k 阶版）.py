# 校园巡检（k 阶版）
# 描述

# 厦门大学翔安校区内有若干个需要巡查的区域。为了保证校园运行安全，后勤巡检员需要从行政楼出发，在校内道路网络中移动，依次经过所有指定区域，完成一轮巡检。

# 校园道路可以抽象成一个带权无向图：

# - 每个点表示一个巡检区域；

# - 每条边表示两个区域之间可以双向通行的道路；

# - 边权表示通过该道路所需的时间。

# 巡检员从 1 号区域出发。每当巡检员到达一个区域时，就认为该区域被巡检了一次，出发时所在的 1 号区域也算已经被巡检了一次。

# 现在要求所有区域都至少被巡检 k 次。任务完成后巡检员可以停在任意区域，不要求回到出发点。

# 请你计算完成任务所需的最短总时间。如果无法完成任务，输出 -1。

# 输入

# 第一行输入三个整数 n, m, k，分别表示区域数、道路数和每个区域至少需要被巡检的次数。

# 接下来 m 行，每行输入三个整数 u, v, w，表示区域 u 和区域 v 之间有一条长度为 w 的双向道路。


# 输出

# 输出一个整数，表示使所有区域都至少被巡检 k 次所需的最短总时间。


# 输入样例 1 

# 2 1 2
# 1 2 5
# 输出样例 1

# 15
# 提示

# 数据范围：

# 2 <= n <= 8

# n - 1 <= m <= 18

# 1 <= k <= 2

# 1 <= w <= 10^4

# 本题目保证图连通。




def algorithm():
    return 0

def test():
    print(algorithm())

def main():
    print(algorithm())

if __name__ == '__main__':
    test()
    main()