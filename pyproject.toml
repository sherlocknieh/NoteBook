[project]
name = "notebook-workspace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "ipykernel==7.2.0",
]

[dependency-groups]
dev = [
    "zensical>=0.0.62",
]
